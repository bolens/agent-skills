interface:
  display_name: "Blast radius"
  short_description: "Prove what a proposed change could break"
  default_prompt: "Use $blast-radius to assess what this change could break."
policy:
  allow_implicit_invocation: false
