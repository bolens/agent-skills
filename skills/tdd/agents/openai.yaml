interface:
  display_name: "Test-Driven Development"
  short_description: "Build behavior through red-green-refactor"
  default_prompt: "Use $tdd to implement this change with the repository's native tests."
