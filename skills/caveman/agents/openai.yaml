interface:
  display_name: "Caveman"
  short_description: "Use an explicit compressed speech mode"
  default_prompt: "Use $caveman in full mode for this conversation."
policy:
  allow_implicit_invocation: false
