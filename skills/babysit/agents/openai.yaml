interface:
  display_name: "Babysit a pull request"
  short_description: "Follow PR audits, CI fixes, and release work"
  default_prompt: "Use $babysit to follow this open PR through a separate audit, actionable nits, CI repairs, and the repository's release playbook where needed."
