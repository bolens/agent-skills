# Contract records

A stack change records its source files, lifecycle, environment key names and roles, storage/network dependencies, generated outputs and owners, selected checks, and operational boundary.

An evidence record stores the check, source revision or example fixture, result, skipped scope, and remaining live verification. It contains no secret values or private deployment endpoints. No persistent database or external interface is introduced.
