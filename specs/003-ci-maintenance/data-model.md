# CI contract records

A check contract records the source revision, events, tested revision, job name, prerequisites, native command, runner/runtime, permissions, artifacts, and whether the check gates merge or delivery.

A shared consumer record adds the selected reusable-workflow ref, inputs, outputs, secrets contract, permissions, and compatibility evidence. Records contain no credential values or private endpoints.
