# Plan

Keep one conditional reference owned by `ci-maintenance`, with short links from
dependency triage, hook setup, release packaging, fleet maintenance, and debugging.
This avoids loading a new coordinator or copying tool installation recipes across
skills. Add the preference to project-owned memory and collection navigation.

Verify current mise, devenv, and Dev Container documentation. Preserve existing
upstream provenance and generated contracts. Validate realistic new-project,
existing-Nix, devcontainer, restricted-PR, and local-diagnosis scenarios, then run
repository gates. Commit with the platform addition because the NixOS and shared
environment guidance cross-reference each other; keep the broader audit separate.
